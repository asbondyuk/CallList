Из нереализованных требований ТЗ:
1) полностью отсутствует тестирование
(в отличии от app, не поднимался контекст - ошибки по подключению к бд,
но сконфигурировать до результата не удалось)
2) фактически не доведено до состояния "прод" (из-за большого числа вопросов по итоговому представлению)


Далее описание логики создания/ возникавшие вопросы.


По бизнесовой части не полностью понятен контекст:
 - как будет использоваться,
 - какие требования к дальнейшему развитию, что, собственно,
 привело к следующему формату (т.е. реализовывалось "по минималке"):
1) один pojo объект (сам Task- были идеи выделить сущность Order, но большой целесообразности не нашел),
2) в контроллере 2 требуемых метода,
3) без HATEOAS (хотя собрать руками/ переделать на Data REST, вроде как не большая проблема).


Отдельные моменты /комментарии:
1) По GET возвращается [] - не знаю на сколько критично
(хотя про необходимость оборачивать в объект для передачи, или
для снижения проблем с десериализаций при обработке другим микросервисом слышал)

2) в POST прилетают отдельные куски данных, что меня смущает

3) конфигурирование: блок вопросов, касаемо передачи параметров.
        если проблемы с JMX - и мои донастройки исключительно из-за моей "инфраструктуры",
        то как конечный пользователь будет прокидывтать параметры подключения к бд, в случае jar- мне не ясно

4) базовый тест на подгрузку контекста удален - не собирался jar-ник (хотя с учетом п.3 потребность в нем под вопросом)

5) формат/тип обработки Date не понятен: какой формат Date или использовать int

6) Создание документации, логгирование, потребность (и формат) обработки ошибок отсутствуют,
т.к. нет представление о требуемом результате (в каком виде отдавать, что обрабатывать)